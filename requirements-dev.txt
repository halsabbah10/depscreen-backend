# Development / test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# RAG evaluation (evaluation / offline experimentation only — not for production)
ragas>=0.4.3

# Linting and security scanning
ruff>=0.4.0
pip-audit>=2.7.0
