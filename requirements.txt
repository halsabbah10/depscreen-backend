# FastAPI and server
# Pinned to the 0.120+ line which depends on starlette>=0.49, closing
# CVE-2025-54121 and CVE-2025-62727. Our code uses stable public surface
# (APIRouter, Depends, BackgroundTasks, exception handlers) unchanged
# since 0.109; the bump is safe.
fastapi>=0.120.0
uvicorn[standard]>=0.27.0
# 0.0.22+ fixes CVE-2024-53981 and CVE-2026-24486 (DoS via large multipart).
python-multipart>=0.0.22

# Database
sqlalchemy>=2.0.27,<2.1
psycopg2-binary>=2.9.0
alembic>=1.12.0

# Authentication
PyJWT>=2.8.0
bcrypt>=4.0.1,<5

# ML/DL
# >=2.6.0 fixes PYSEC-2025-41 (RCE via torch.load weights_only=True).
torch>=2.6.0,<3
# 4.50+ closes a long chain of CVEs (PYSEC-2024-227/228/229, CVE-2025-*).
# DistilBERT tokenizer + AutoModelForSequenceClassification APIs we use
# are stable across this range.
transformers>=4.50.0,<5.0.0
huggingface-hub>=0.20.0
numpy>=1.24.0,<3
pandas>=2.0.0
scikit-learn>=1.3.0

# LLM Integration (OpenAI-compatible API)
openai>=1.30.0
tenacity>=8.2.0

# RAG (Phase 5)
pgvector>=0.2.0
sentence-transformers>=2.2.0

# PDF parsing — Docling is primary; pdfplumber is the fallback for simpler PDFs
pdfplumber>=0.11.0

# Document extraction — Docling handles complex clinical PDF layouts
docling>=2.0.0

# Backend Hardening
slowapi>=0.1.9
redis>=5.0.0

# Email notifications
resend>=0.8.0
jinja2>=3.1.0
# Resend webhook signature verification (Svix-compatible)
svix>=1.20.0

# Background scheduler (screening/appointment reminder jobs)
apscheduler>=3.10.0,<4

# Utilities
PyYAML>=6.0
python-dotenv==1.2.2
# pydantic >=2.11 required by svix>=1.20 (ModelWrapValidatorHandler import)
# and by fastapi>=0.120. Removing the hard pin lets the resolver pick a
# version compatible with both upstreams.
pydantic>=2.11,<3
pydantic-settings>=2.1.0
httpx==0.26.0

# X/Twitter integration (unofficial GraphQL client — cookie-based auth)
twikit>=2.3.0

# PDF generation (screening result reports, patient export, clinical summary)
reportlab>=4.0.0

# Supabase Storage (avatar uploads) + image normalisation
supabase>=2.0.0
# >=12.2.0 fixes CVE-2026-25990 (PSD OOB write) and CVE-2026-40192 (FITS decompression bomb).
Pillow>=12.2.0,<13

# Error monitoring
sentry-sdk[fastapi]>=2.0.0
